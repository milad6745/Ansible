در انسیبل (Ansible)، مفهوم **"idempotency"** یا **"ایدوپوتنسی" (ایدِم‌پوتنسی)** یکی از اصول کلیدی در طراحی و اجرای playbookها و taskهاست. این ویژگی به معنای **"تغییرناپذیری نتیجه اجرای مکرر یک عملیات"** است.

---

### تعریف ساده:

اگر یک task در انسیبل **چند بار اجرا شود**، **نتیجه‌ی نهایی باید یکسان باشد** و سیستم نباید با هر بار اجرا تغییر جدیدی بکند، **مادامی‌ که وضعیت سیستم تغییر نکرده باشد.**

---

### مثال ساده:

فرض کنید یک task در انسیبل دارید که یک پکیج (مثلاً `nginx`) را نصب می‌کند:

```yaml
- name: نصب nginx
  apt:
    name: nginx
    state: present
```

این task:

* اگر `nginx` نصب نشده باشد → آن را نصب می‌کند.
* اگر قبلاً نصب شده باشد → هیچ کاری نمی‌کند.

📌 این رفتار همان **idempotency** است: اجرای دوباره‌ی task منجر به تغییر جدید نمی‌شود اگر وضعیت سیستم از قبل همان باشد.

---

### چرا idempotency مهم است؟

1. **اطمینان از قابل پیش‌بینی بودن اجرای playbook**
2. **کاهش ریسک اعمال تغییرات ناخواسته**
3. **امکان اجرای playbook در هر زمان، بدون نگرانی از خراب شدن سیستم**
4. **برنامه‌نویسی و اتوماسیون قابل اطمینان**

---

### انسیبل چگونه idempotency را حفظ می‌کند؟

انسیبل ماژول‌هایی مانند `apt`, `yum`, `file`, `service`, `copy`, `template` و... دارد که به‌طور **ذاتی idempotent هستند**. آن‌ها قبل از اعمال تغییر، وضعیت فعلی سیستم را بررسی می‌کنند.

مثال:

```yaml
- name: اطمینان از فعال بودن سرویس nginx
  service:
    name: nginx
    state: started
    enabled: true
```

اگر nginx هم‌اکنون در حال اجرا باشد و در boot فعال شده باشد، اجرای این task چیزی را تغییر نخواهد داد.

---

### موارد غیر idempotent

برخی taskها یا اسکریپت‌ها ممکن است **غیر idempotent** باشند. مثلاً:

```yaml
- name: اجرای یک اسکریپت Bash
  shell: ./setup.sh
```

اگر `setup.sh` طوری طراحی نشده که هر بار اجرا نتیجه‌ای یکسان بدهد، اجرای مکرر آن ممکن است مشکلاتی ایجاد کند.

✅ راه‌حل: استفاده از `creates`, `when`, `changed_when`, `check mode` و شرایط منطقی برای مدیریت اجرای مکرر.

---

### نکات تکمیلی:

* همیشه سعی کنید taskهای خود را **idempotent** بنویسید.
* از **ماژول‌های رسمی انسیبل** استفاده کنید، چون اغلب idempotent هستند.
* از `register` و `when` برای شرطی کردن اجرای دستورات استفاده کنید.

---
